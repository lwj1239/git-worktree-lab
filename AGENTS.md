# AGENTS.md

## 项目概览
- 项目名称：git-worktree-lab。
- 项目目标：通过最小练习项目验证 GitHub、Branch、Worktree、PR、CI、人工评审和 Squash Merge 的完整协作流程。
- 当前阶段：通用仓库初始化；功能及 CI 通过后续任务 PR 添加。

## 工作约定
- 开始修改前先阅读 README、相关文档和目标代码。只修改当前任务必需的文件。
- 主 Worktree 固定保持 main，仅用于同步、查看和管理；初始化完成后的代码修改必须走任务分支和 PR。
- 一个任务对应一个分支、一个独立 Worktree 和一个 PR；并行任务不得共享工作目录。
- 分支使用英文小写及 feat/、fix/、docs/、chore/ 等前缀；Commit、PR 标题和说明以中文为主。
- Review 修改继续使用原分支、Worktree 和 PR。同步主线使用 git fetch origin 和 git merge origin/main，在任务分支解决冲突并重新验证。
- 不读取、输出或提交密钥、令牌、个人数据、.env 或缓存。
- 新增生产依赖、变更数据库结构、删除数据、重写 Git 历史或执行未授权的外部操作前，说明影响并取得确认；已有明确授权不重复询问。
- 后续 Python 开发使用 uv 管理环境和依赖，pytest 验证行为，Ruff 执行代码检查。

## 合并与交付
- 只有当前 PR 最新提交的 CI 全部通过，且用户明确评审通过并授权合并后，才可执行 Squash Merge 到 main。
- 不自行认定用户已经评审通过，不启用自动合并；不要求用户另找独立账号提交 GitHub Approve。
- 评审后若代码再次修改，应重新通过 CI 并交给用户确认。
- 合并后确认无未提交或需保留的工作，再清理远程任务分支、同步 main、删除任务 Worktree 和本地任务分支。
- 修改后执行相关验证；未能运行的检查明确说明。交付说明修改、验证结果和未完成事项。
